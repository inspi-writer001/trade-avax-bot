import { ethers } from "ethers";

export const useEthers = async (
  userAddress,
  contractAddress,
  mnemonics,
  decimal,
  ticker,
  coinName,
  amount,
  slippage
) => {};

console.log(Date.now() + 1000 * 60 * 10);
